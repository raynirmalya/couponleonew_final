CouponLeo traffic fixes: deployed September 7, 2026 (Asia/Calcutta)

Store/category coupons appear in initial server HTML and precede explanatory content. Direct API queries avoid full catalog loading for coupon and unknown-store lookups. Offer responses suppress expired-date, conflicting-percentage and duplicate/banner-variant records without changing stored feed rows. Source counts/pagination are retained, so quality filtering can produce fewer than 12 cards. Unknown expiry remains unconfirmed; feed entries are not described as checkout-tested. Date-only browser expiry labels use the API UTC day boundary.

Coupon reveal, merchant links for offers without codes, saved-item controls, filters, pagination, authentication routes and analytics remain available. English content is canonical while partial locale navigation stays accessible. Homepage aliases redirect, unknown routes return 404, and the current sitemap contains 4,510 stores. Google verification, robots and favicon assets are preserved. The server request interceptor preserves market and pagination queries through HTTPS rendering.

Validated: 40 frontend tests, 7 backend tests, production build, 15 route/static checks including production Host headers, and mobile Chrome workflows. Browser checks cover initial HTML cards, hydration, horizontal overflow, code/offer dialogs, pagination, India market filtering and Google Analytics HTTP 204 collection. These are synthetic checks, not field Core Web Vitals or evidence of ranking growth.

Current UI: /srv/couponleo-ui/current -> /srv/couponleo-ui/releases/20260907-expiry-labels. Final delta SHA-256: 185d13b90a60945754c1db7da6656ae32a6178dc7fc92466596a9931ac6bf8ee. Source is committed in /root/code/couponleonew_final. Systemd runs couponleo-ui.service on 4173 and cpleo-api.service on 9600 using the committed drop-in templates. Nginx continues to use these canonical ports. The duplicate enabled Nginx backup file was moved into the backup directory.

Rollout kept the tested preview serving traffic while canonical services restarted. The one-time rollout script records the initial deployment procedure; do not rerun it against an existing release. Run python3 deploy/check-release.py https://couponleo.com for read-only route checks.

Rollback material: /root/backups/couponleo-traffic-fix-20260906 contains original Git bundle, API source, private environment backup, systemd units and Nginx configuration. Old UI builds remain available. An immediate UI rollback can point current to the previous query-filters release and restart the UI; this retains the API fixes. A full pre-change rollback must restore original source, service overrides and built UI together. Test alternate ports before switching Nginx. Never commit private environment backups.

Merchant checkout validity, Search Console indexing/queries/backlinks and future traffic improvement remain unverified. Some unknown-store lookups are still slow, and additional content/asset improvements remain possible.
